# external-secrets/external-secrets 2.7.0 (https://charts.external-secrets.io)
# installCRDs: true is already the chart default, so this file has no
# effective overrides. Installed here unused in dev — the prod overlay is
# what actually wires up an ExternalSecret/SecretStore against a real vault;
# dev keeps the operator present so the chart shape doesn't diverge later.
