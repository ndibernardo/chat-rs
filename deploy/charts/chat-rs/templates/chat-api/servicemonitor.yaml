{{- if .Values.monitoring.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: chat-api
  labels:
    {{- include "chat-rs.labels" (dict "root" $ "workload" "chat-api") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "chat-rs.selectorLabels" (dict "root" $ "workload" "chat-api") | nindent 6 }}
  endpoints:
    - port: metrics
      interval: 30s
{{- end }}
