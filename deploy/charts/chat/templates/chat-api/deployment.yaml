apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-api
  labels:
    {{- include "chat.labels" (dict "root" $ "workload" "chat-api") | nindent 4 }}
spec:
  # Omitted, not defaulted, when the HPA owns this field — reasserting a
  # static replica count on every helm upgrade would fight the HPA's own
  # scaling decisions.
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.chatApi.replicas }}
  {{- end }}
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      {{- include "chat.selectorLabels" (dict "root" $ "workload" "chat-api") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "chat.selectorLabels" (dict "root" $ "workload" "chat-api") | nindent 8 }}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      {{- include "chat.topologySpreadConstraints" (dict "root" $ "workload" "chat-api") | nindent 6 }}
      terminationGracePeriodSeconds: 30
      containers:
        - name: chat-api
          image: "{{ .Values.images.chat.repository }}:{{ .Values.images.chat.tag }}"
          imagePullPolicy: {{ .Values.images.chat.pullPolicy }}
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          ports:
            - name: http
              containerPort: 3002
            - name: metrics
              containerPort: 9090
          env:
            - name: SERVICE_ROLE
              value: api
            {{- include "chat.commonEnv" . | nindent 12 }}
            {{- include "chat.chatCoreEnv" . | nindent 12 }}
            - name: JWT__PUBLIC_KEY_PATH
              value: /etc/chat/jwt/jwt_ed25519.pub.pem
          volumeMounts:
            - name: jwt-public-key
              mountPath: /etc/chat/jwt
              readOnly: true
          livenessProbe:
            httpGet:
              path: /livez
              port: 3002
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 3002
            periodSeconds: 5
            failureThreshold: 2
          startupProbe:
            httpGet:
              path: /livez
              port: 3002
            periodSeconds: 2
            failureThreshold: 30
          lifecycle:
            preStop:
              # Gives the endpoint controller time to remove this pod from
              # the Service before the container actually stops accepting
              # connections, avoiding a burst of connection-refused errors.
              exec:
                command: ["sh", "-c", "sleep 5"]
          resources:
            {{- toYaml .Values.chatApi.resources | nindent 12 }}
      volumes:
        - name: jwt-public-key
          configMap:
            name: jwt-public-key
