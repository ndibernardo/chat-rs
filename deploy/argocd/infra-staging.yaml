apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: chat-infra-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/ndibernardo/chat.git
    targetRevision: main
    path: deploy/charts/chat-infra
    helm:
      valueFiles:
        - values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: chat-staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: false
    syncOptions:
      - CreateNamespace=true
